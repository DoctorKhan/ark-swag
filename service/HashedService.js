'use strict';


/**
 * Create hashed
 * This can only be done by the logged in user.
 *
 * body Borrower Created hashed object
 * no response value expected for this operation
 **/
exports.createUser = function(body) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

